# 安全策略 / Security Policy

## 支持版本

| 版本 | 支持状态 |
|---|---|
| main 分支 | ✅ 维护中 |

## 报告漏洞

请勿在公开 issue 中披露安全漏洞。

- **邮件反馈**：请在仓库 Issues 中以 "Security" 标签创建**私密**反馈，或通过项目维护者的企业邮箱联系。
- **反馈内容建议**：影响范围、复现步骤（含内核版本与部署方式）、PoC（如涉敏感数据请脱敏）、修复建议。
- **响应时效**：我们会在 7 个工作日内确认收到，并在确认后及时给出修复计划与版本安排。

## 安全使用边界

MemGuard 依赖 eBPF kprobe 采集进程写入行为，需 root 或 `CAP_BPF` + `CAP_PERFMON` 权限。请仅在**自有或已明确授权**的主机/容器上部署。滥用本工具对未经授权的系统进行监控，可能违反所在司法辖区的法律法规。

## 数据说明

告警载荷中包含进程名（comm）、PID、写入文件路径与命中签名，不包含写入内容的完整副本。企业微信/ServerChan 密钥请通过 `/etc/memguard.env` 配置（权限 `0644` 或更严格），切勿提交至代码仓库。
